# ── KAFKA ──────────────────────────────────────────────────
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# ── REDDIT ─────────────────────────────────────────────────
# No credentials needed — uses Reddit's public JSON API

# ── YOUTUBE DATA API ───────────────────────────────────────
# Get this at: https://console.cloud.google.com
YOUTUBE_API_KEY=your_youtube_api_key

# ── NEWS API ───────────────────────────────────────────────
# Get this at: https://newsapi.org
NEWS_API_KEY=your_news_api_key

# ── FLINK ──────────────────────────────────────────────────
FLINK_JOBMANAGER_HOST=localhost
FLINK_JOBMANAGER_PORT=8081

# ── LLM (llama.cpp) ────────────────────────────────────────
# llama.cpp server endpoint - run with: llama-server -m phi-4-mini.gguf
LLAMA_SERVER_URL=http://localhost:8080

# ── DATABASE ───────────────────────────────────────────────
SQLITE_DB_PATH=./data/zeitgeist.db

# ── PIPELINE SETTINGS ──────────────────────────────────────
SLIDING_WINDOW_SIZE_SECONDS=300    # 5 minute window
SLIDING_WINDOW_SLIDE_SECONDS=30    # Slide every 30 seconds
MAX_SAMPLE_TEXTS=5                 # Max texts sent to LLM per signal
POLL_INTERVAL_SECONDS=60           # How often producers poll APIs
SCORER_WORKERS=4                   # Parallel threads for sentiment scoring
