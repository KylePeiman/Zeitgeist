# Kafka
confluent-kafka==2.4.0

# Reddit
praw==7.7.1

# YouTube
google-api-python-client==2.131.0

# News
newsapi-python==0.2.7
feedparser==6.0.11   # Google News RSS parsing (news_producer Phase 2)

# Flink
apache-flink==1.18.1

# NLP / NER
spacy==3.7.4
# spaCy English model — REQUIRED for NER entity discovery. Without it
# spacy.load("en_core_web_sm") fails, NLP_AVAILABLE is False, and the pipeline
# can only ever track the seed entities (the cause of the "stuck at 94" plateau).
# Pinned to a 3.7.x model wheel compatible with spacy 3.7.4. If the direct URL
# is unavailable in your environment, run: python -m spacy download en_core_web_sm
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
vaderSentiment==3.3.2

# HTTP
requests==2.31.0
httpx==0.27.0

# Database (Neon Postgres)
psycopg2-binary==2.9.9

# Data
pandas==2.2.2

# Dashboard
streamlit==1.35.0
plotly==5.22.0

# Utils
python-dotenv==1.0.1
loguru==0.7.2
